# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'fongo' do
  developer('JakubOboza', 'jakub.oboza@gmail.com')

  # self.rubyforge_name = 'fongox' # if different than 'fongo'
end

# use emacs!
